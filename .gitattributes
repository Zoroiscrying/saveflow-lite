# Normalize EOL for all text files and exclude everything from archive by default.
* text=auto eol=lf export-ignore

# Keep only plugin payload for Godot Asset Library archive downloads.
addons/** -export-ignore
